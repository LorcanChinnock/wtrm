name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash -n bin/wtrm.sh
      - uses: ludeeus/action-shellcheck@master
        with:
          scandir: bin
